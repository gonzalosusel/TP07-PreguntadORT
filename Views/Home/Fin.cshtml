@{
    ViewData["title"] = "¡El fin!";
}

<div class="text-center formulario">    
    <table class="table table-dark">
        <tr><td>Tu nombre de usuario: </td><td>@ViewBag.DatosUsuario.Item1</td></tr>
        <tr><td>Tu puntaje final: </td><td>@ViewBag.PuntajeActual</td></tr>
    </table>

    <a href='@Url.Action("ConfigurarJuego", "Home")'>
        <button class="btn btn-primary">Reiniciar partida</button>
    </a>
</div>